name: Public IP
description: Query ident.me to check my current IP (ensure VPN is working)
version: 0.2
exec:
  - python
  - "{pluginDir}/myIp.py"
interface: raw
tasks:
  - name: Get my IP
    description: Query ident.me to check my current IP (ensure VPN is working)
